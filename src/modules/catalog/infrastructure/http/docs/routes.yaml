tags:
- name: Attributes
  description: Gerenciamento de atributos do catálogo
- name: Categories
  description: Gerenciamento de categorias do catálogo
- name: Images
  description: Gerenciamento de imagens dos produtos
- name: Produtos
  description: Gerenciamento de produtos

components:
  schemas:
    ErrorResponseDTO:
      type: object
      properties:
        status:
          type: string
          example: error
        errors:
          type: array
          items:
            type: string
          example: [ "Mensagens de erro detalhadas aqui" ]

  responses:
    StandardError:
      description: Resposta de erro padrão
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponseDTO'

paths:
  # --- Atributos ---
  /catalog/api/v1/attributes:
    get:
      summary: Lista todos os atributos
      tags: [ Attributes ]
      parameters:
      - in: query
        name: name
        schema:
          type: string
      - in: query
        name: limit
        schema:
          type: integer
          default: 10
      - in: query
        name: page
        schema:
          type: integer
          default: 1
      responses:
        200:
          description: Lista de atributos retornada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAttributesDTO'
        400: { $ref: '#/components/responses/StandardError' }
        500: { $ref: '#/components/responses/StandardError' }
    post:
      summary: Cria um novo atributo
      tags: [ Attributes ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAttributeDTO'
      responses:
        201:
          description: Atributo criado com sucesso
        400: { $ref: '#/components/responses/StandardError' }

  /catalog/api/v1/attributes/{id}:
    get:
      summary: Obtém um atributo pelo ID
      tags: [ Attributes ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    patch:
      summary: Atualiza um atributo existente
      tags: [ Attributes ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAttributeDTO'
    delete:
      summary: Remove um atributo
      tags: [ Attributes ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      responses:
        204:
          description: Atributo removido com sucesso

  # --- Categorias ---
  /catalog/api/v1/categories:
    get:
      summary: Lista todas as categorias
      tags: [ Categories ]
      responses:
        200:
          description: Lista de categorias retornada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedCategoriesDTO'
    post:
      summary: Cria uma nova categoria
      tags: [ Categories ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryDTO'
      responses:
        201:
          description: Categoria criada com sucesso

  /catalog/api/v1/categories/{id}:
    get:
      summary: Obtém uma categoria pelo ID
      tags: [ Categories ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    patch:
      summary: Atualiza uma categoria existente
      tags: [ Categories ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid

  # --- Imagens ---
  /catalog/api/v1/images:
    get:
      summary: Lista imagens de forma paginada
      tags: [ Images ]
      parameters:
      - in: query
        name: limit
        schema:
          type: integer
          default: 10
      - in: query
        name: page
        schema:
          type: integer
          default: 1
      responses:
        200:
          description: Lista de imagens retornada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedImagesDTO'
        400: { $ref: '#/components/responses/StandardError' }

    post:
      summary: Cria uma nova imagem para um produto
      tags: [ Images ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateImageDTO'
      responses:
        201:
          description: Imagem criada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageResponseDTO'
        400: { $ref: '#/components/responses/StandardError' }

  /catalog/api/v1/images/{id}:
    get:
      summary: Obtém uma imagem pelo ID
      tags: [ Images ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      responses:
        200:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageResponseDTO'
        404: { $ref: '#/components/responses/StandardError' }

    patch:
      summary: Atualiza dados de uma imagem (url ou ordem)
      tags: [ Images ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateImageDTO'
      responses:
        200:
          description: Imagem atualizada com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImageResponseDTO'
        400: { $ref: '#/components/responses/StandardError' }

    delete:
      summary: Remove uma imagem do catálogo
      tags: [ Images ]
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
      responses:
        204:
          description: Imagem removida com sucesso
        404: { $ref: '#/components/responses/StandardError' }

  # --- Produtos ---
  /catalog/api/v1/products:
    post:
      summary: Criar um novo produto
      tags: [ Produtos ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProductInputDTO'
      responses:
        '201':
          description: Produto criado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductOutputDTO'
        '400':
          $ref: '#/components/responses/StandardError'
        '404':
          $ref: '#/components/responses/StandardError'
        '409':
          $ref: '#/components/responses/StandardError'

    get:
      summary: Listar produtos com paginação e filtros
      tags: [ Produtos ]
      parameters:
      - in: query
        name: page
        schema: { type: integer, minimum: 1, default: 1 }
      - in: query
        name: limit
        schema: { type: integer, minimum: 1, default: 10 }
      - in: query
        name: name
        schema: { type: string }
      - in: query
        name: slug
        schema: { type: string }
      - in: query
        name: description
        schema: { type: string }
      responses:
        '200':
          description: Lista de produtos retornada com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/ProductOutputDTO'
                  total: { type: integer }
                  page: { type: integer }
                  limit: { type: integer }
        '400':
          $ref: '#/components/responses/StandardError'

  /catalog/api/v1/products/{id}:
    parameters:
    - in: path
      name: id
      required: true
      schema: { type: string, format: uuid }

    get:
      summary: Obter produto por ID
      tags: [ Produtos ]
      responses:
        '200':
          description: Detalhes do produto
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductOutputDTO'
        '404':
          $ref: '#/components/responses/StandardError'

    put:
      summary: Atualizar um produto existente
      tags: [ Produtos ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProductInputDTO'
      responses:
        '200':
          description: Produto atualizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProductOutputDTO'
        '400':
          $ref: '#/components/responses/StandardError'
        '404':
          $ref: '#/components/responses/StandardError'
        '409':
          $ref: '#/components/responses/StandardError'

    delete:
      summary: Remover um produto
      tags: [ Produtos ]
      responses:
        '204':
          description: Produto removido com sucesso
        '404':
          $ref: '#/components/responses/StandardError'
        '400':
          $ref: '#/components/responses/StandardError'

  # --- Skus ---
  /catalog/api/v1/skus:
    post:
      summary: Cria um novo SKU
      tags: [ SKUs ]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSkuInputDTO'
      responses:
        '201':
          description: SKU criado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuDetailsOutputDTO'
        '404':
          $ref: '#/components/responses/StandardError'
        '409':
          $ref: '#/components/responses/StandardError'

  /catalog/api/v1/skus/{id}:
    get:
      summary: Busca SKU por ID
      tags: [ SKUs ]
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Detalhes do SKU
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuDetailsOutputDTO'
        '404':
          $ref: '#/components/responses/StandardError'

    delete:
      summary: Remove um SKU
      tags: [ SKUs ]
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '24':
          description: SKU removido com sucesso
        '404':
          $ref: '#/components/responses/StandardError'

  /catalog/api/v1/skus/product/{productId}:
    get:
      summary: Lista todos os SKUs de um produto
      tags: [ SKUs ]
      parameters:
      - name: productId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Lista de SKUs vinculados ao produto
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SkuDetailsOutputDTO'

  /catalog/api/v1/skus/{id}/price:
    patch:
      summary: Atualiza o preço de um SKU
      tags: [ SKUs ]
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: './schemas.yaml#/components/schemas/UpdatePriceInputDTO'
      responses:
        '200':
          description: Preço atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuDetailsOutputDTO'

  /catalog/api/v1/skus/{id}/logistics:
    patch:
      summary: Atualiza peso e dimensões de um SKU
      tags: [ SKUs ]
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: './schemas.yaml#/components/schemas/UpdateLogisticsInputDTO'
      responses:
        '200':
          description: Dados logísticos atualizados
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuDetailsOutputDTO'

  /catalog/api/v1/sku-attributes:
    post:
      summary: Atribuir valor de atributo a um SKU
      tags:
      - SkuAttributeValue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSkuAttributeValueRequestDTO'
      responses:
        '201':
          description: Criado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuAttributeValueResponseDTO'
        '400':
          $ref: '#/components/responses/StandardError'
        '409':
          $ref: '#/components/responses/StandardError'

  /catalog/api/v1/sku-attributes/sku/{skuId}:
    get:
      summary: Lista todos os atributos de um SKU específico
      tags:
      - SkuAttributeValue
      parameters:
      - name: skuId
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '200':
          description: Lista de atributos retornada
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SkuAttributeValueResponseDTO'
        '400':
          $ref: '#/components/responses/StandardError'

  /catalog/api/v1/sku-attributes/{id}:
    put:
      summary: Atualiza o valor de um atributo de SKU
      tags:
      - SkuAttributeValue
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSkuAttributeValueRequestDTO'
      responses:
        '200':
          description: Atualizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkuAttributeValueResponseDTO'
        '400':
          $ref: '#/components/responses/StandardError'
        '404':
          $ref: '#/components/responses/StandardError'

    delete:
      summary: Remove um atributo de um SKU
      tags:
      - SkuAttributeValue
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      responses:
        '204':
          description: Removido com sucesso
        '404':
          $ref: '#/components/responses/StandardError'
        '500':
          $ref: '#/components/responses/StandardError'
